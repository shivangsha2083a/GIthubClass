using UnityEngine;
[CreateAssetMenu(fileName = "Playerdata", menuName = "Playerdata/Playerdata2")]
public class Playerdata2 : Playerdata
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
