using UnityEngine;
[CreateAssetMenu(fileName = "PlayerData2", menuName = "PlayerData/PlayerData2")]

public class PlayerData2 : PlayerData
{
    public PlayerData playerdata;
    void Start()
    {
               
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
