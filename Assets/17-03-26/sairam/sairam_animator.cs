using UnityEngine;

public class sairam_animator : MonoBehaviour
{
   
    void Update()
    {
        if(Input.GetKeyDown(KeyCode.A))
        {
            //do the transitiont to first animation
        }
    }
}
